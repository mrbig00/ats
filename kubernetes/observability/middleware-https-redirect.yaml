# HTTP → HTTPS for Grafana only (do not attach to IngressClass globally).
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: grafana-https-redirect
  namespace: observability
spec:
  redirectScheme:
    scheme: https
    permanent: true
